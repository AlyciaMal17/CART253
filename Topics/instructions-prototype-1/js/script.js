/**
 * Prototype Project 1
 * Alycia Malonga
 * 
 * Working on the three prototype projects for the first assignment in the course.
 * Prototype 1 example;
 */

"use strict";

/**
 * creating the canvas for the prototype
 */
function setup() {
// Create a canvas to draw on
createCanvas(640, 480);
}

/**
 * Draws the background and needed shape for the project example 1
 */
function draw() {
// Set the background color
background(255, 100, 100);
// Draw a rectangle in the center of the canva
rect(200, 80, 240, 320);
}